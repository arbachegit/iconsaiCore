name: Deploy to DigitalOcean

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_health_check:
        description: 'Skip health check after deploy'
        required: false
        default: 'false'
        type: boolean

env:
  NODE_VERSION: '20'
  DEPLOY_PATH: /var/www/core.iconsai.ai
  RETENTION_COUNT: 3  # Numero de backups a manter

jobs:
  # ============================================
  # JOB 1: BUILD
  # Compila o projeto no GitHub Actions (nao no servidor)
  # ============================================
  build:
    name: Build Application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies (apps/web)
        working-directory: apps/web
        run: |
          rm -rf node_modules package-lock.json
          npm install --legacy-peer-deps

      - name: Run pre-deploy checks
        run: npm run pre-deploy || true

      - name: Build application (Next.js)
        working-directory: apps/web
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_VOICE_API_URL: ${{ secrets.NEXT_PUBLIC_VOICE_API_URL }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: |
            apps/web/.next/
            apps/web/public/
            apps/web/package.json
            apps/web/next.config.*
          retention-days: 7

  # ============================================
  # JOB 2: DEPLOY
  # Envia os arquivos compilados para o servidor
  # ============================================
  deploy:
    name: Deploy to Server
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: build/

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Create backup on server
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'ENDSSH'
            DEPLOY_PATH="/var/www/core.iconsai.ai"
            BACKUP_DIR="/var/www/backups"
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)

            # Criar diretorio de backups se nao existir
            mkdir -p $BACKUP_DIR

            # Fazer backup do deploy atual (se existir)
            if [ -d "$DEPLOY_PATH/.next" ]; then
              echo "Creating backup: backup_$TIMESTAMP"
              cp -r $DEPLOY_PATH $BACKUP_DIR/backup_$TIMESTAMP
            fi

            # Manter apenas os ultimos 3 backups
            cd $BACKUP_DIR
            ls -dt backup_* 2>/dev/null | tail -n +4 | xargs rm -rf 2>/dev/null || true

            echo "Backup completed. Current backups:"
            ls -la $BACKUP_DIR
          ENDSSH

      - name: Deploy via rsync
        run: |
          rsync -avzr --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            build/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ env.DEPLOY_PATH }}/

      - name: Install production dependencies and restart
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'ENDSSH'
            cd /var/www/core.iconsai.ai
            npm install --production --legacy-peer-deps 2>/dev/null || true
            chmod -R 755 /var/www/core.iconsai.ai
            echo "=== Restarting application ==="
            sudo systemctl restart iconsai-web 2>/dev/null || sudo systemctl reload nginx || sudo nginx -s reload || true
            echo "Deploy completed and application restarted"
          ENDSSH

      - name: Cleanup SSH key
        if: always()
        run: rm -f ~/.ssh/deploy_key

  # ============================================
  # JOB 3: VERIFY
  # Verifica se o deploy funcionou
  # ============================================
  verify:
    name: Verify Deployment
    needs: deploy
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.skip_health_check != 'true' }}

    steps:
      - name: Wait for server to stabilize
        run: sleep 10

      - name: Health check
        run: |
          SITE_URL="${{ secrets.SITE_URL }}"

          if [ -z "$SITE_URL" ]; then
            echo "SITE_URL secret not configured, skipping health check"
            exit 0
          fi

          echo "Checking $SITE_URL..."

          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$SITE_URL" --max-time 30)

          if [ "$HTTP_STATUS" -eq 200 ] || [ "$HTTP_STATUS" -eq 301 ] || [ "$HTTP_STATUS" -eq 302 ]; then
            echo "Health check passed! Status: $HTTP_STATUS"
          else
            echo "Health check failed! Status: $HTTP_STATUS"
            exit 1
          fi

      - name: Deployment summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Deployed by** | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Timestamp** | $(date -u '+%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Deployment completed successfully!" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # JOB 4: NOTIFY (em caso de falha)
  # ============================================
  notify-failure:
    name: Notify on Failure
    needs: [build, deploy, verify]
    runs-on: ubuntu-latest
    if: failure()

    steps:
      - name: Create failure summary
        run: |
          echo "## Deployment Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered by** | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Timestamp** | $(date -u '+%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Check the logs above for details." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Rollback Instructions" >> $GITHUB_STEP_SUMMARY
          echo "To rollback, run this on the server:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "cd /var/www" >> $GITHUB_STEP_SUMMARY
          echo "ls backups/  # List available backups" >> $GITHUB_STEP_SUMMARY
          echo "sudo systemctl stop iconsai-web" >> $GITHUB_STEP_SUMMARY
          echo "rm -rf core.iconsai.ai && cp -r backups/backup_YYYYMMDD_HHMMSS core.iconsai.ai" >> $GITHUB_STEP_SUMMARY
          echo "sudo systemctl start iconsai-web" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
